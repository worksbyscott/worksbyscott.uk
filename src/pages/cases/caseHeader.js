import react from 'react';

const caseHeader = (slide) => {

    const title = slide['title'],
        information = slide['information'],
        tools = slide['information'],
        client = slide['client'],
        type = slide['type']


    return (
        <>

        </>
    )
}