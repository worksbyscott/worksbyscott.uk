import React from "react";
import CaseWrapper from '../../components/case/caseWrapper'

const CellaPage = (index) => {
  return (

    <CaseWrapper index={index}>
      <h1>TEst</h1>
    </CaseWrapper>

  );
};

export default CellaPage;
